export class PathConfig {
    static Home = "/";
    static About = "/about";
    static Drag = "/test";
    static Pixi = "/pixi";
    static Typescript = "/typescript";
}
//readonly 属性 赋值后再也不能被改变
interface Point {
    readonly x: number;
}

